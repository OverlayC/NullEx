# NullEx
an external unity mod injector with some gui

it uses SharpMonoInjector for rn but at some point ill update it to be able to use Il2cpp and Mono with a bridge
im going to add multi dll injection and make so melon loader and bepinex mods work with it (downloading runtimes then injecting before injecting the mod)
